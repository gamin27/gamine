---
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import ShareButton from '../components/ShareButton.astro';
import TableOfContents from '../components/TableOfContents.astro';
import TagLink from '../components/TagLink.astro';

type Heading = {
	depth: number;
	slug: string;
	text: string;
};

type Props = CollectionEntry<'blog'>['data'] & {
	headings?: Heading[];
};

const { title, description, pubDate, updatedDate, tags = [], headings = [] } = Astro.props;
const hasTableOfContents = headings.some((heading) => heading.depth === 2);
---

<html lang="ja">
	<head>
		<BaseHead
			title={title}
			description={description}
			imageAlt={`${title} | gamine.blog`}
			type="article"
			publishedTime={pubDate}
			modifiedTime={updatedDate}
			tags={tags}
		/>
		<style>
			html {
				background: var(--bg);
			}
			main {
				width: 100%;
				max-width: none;
				margin: 0;
				padding: 0;
			}
			.page-frame {
				min-height: 100vh;
				background: var(--bg);
				transition: transform 0.24s ease;
			}
			.article-shell {
				display: grid;
				grid-template-columns: minmax(180px, 240px) minmax(0, 720px);
				gap: 48px;
				width: min(100% - 64px, 1008px);
				margin: auto;
				padding: 48px 0;
			}
			.toc-desktop {
				position: sticky;
				top: 96px;
				align-self: start;
				padding-top: 4px;
			}
			.toc-mobile {
				display: none;
			}
			.mobile-toc-toggle,
			.mobile-toc-toggle-state {
				display: none;
			}
			.mobile-page-underlay {
				display: none;
			}
			.mobile-page-dismiss {
				display: none;
			}
			.prose {
				width: 100%;
				min-width: 0;
				color: rgb(var(--gray-dark));
				font-size: 18px;
			}
			.title {
				margin-bottom: 2rem;
				padding: 0 0 1rem;
				text-align: left;
				line-height: 1.4;
			}
			.title h1 {
				margin: 0;
				font-size: 32px;
				line-height: 1.4;
			}
			.date {
				margin-top: 4px;
				color: rgb(var(--gray));
				font-size: 12px;
				line-height: 1.4;
			}
			.last-updated-on {
				font-style: italic;
			}
			.tags {
				display: flex;
				flex-wrap: wrap;
				justify-content: flex-start;
				margin-top: 1rem;
			}
			.prose :global(h2) {
				scroll-margin-top: 112px;
				padding-bottom: 0.35rem;
				border-bottom: 1px solid rgba(var(--gray), 0.35);
				font-size: 24px;
			}
			.prose :global(h3) {
				font-size: 22px;
			}
			.prose :global(h4) {
				font-size: 20px;
			}
			.prose :global(h5) {
				font-size: 18px;
			}
			.prose :global(h6) {
				font-size: 16px;
			}
			.prose :global(pre),
			.prose :global(.astro-code) {
				border: 1px solid rgba(var(--gray), 0.2);
			}
			.prose :global(blockquote) {
				border-left-color: rgb(var(--gray));
			}
			@media (max-width: 960px) {
				.article-shell {
					display: block;
					width: calc(100% - 32px);
					padding: 16px 0;
				}
				.page-frame {
					position: relative;
					z-index: 3;
				}
				body:has(#mobile-toc-toggle-state:checked) .page-frame,
				body:has(#mobile-toc-toggle-state:checked) .mobile-page-underlay {
					transform: translateX(min(80vw, 320px));
				}
				body:has(#mobile-toc-toggle-state:checked) .page-frame {
					box-shadow: -12px 0 32px rgba(var(--gray-dark), 0.28);
				}
				.toc-desktop {
					display: none;
				}
				.mobile-page-underlay {
					position: fixed;
					inset: 0;
					z-index: 2;
					display: block;
					background: var(--bg);
					transition: transform 0.24s ease;
					pointer-events: none;
				}
				.mobile-page-dismiss {
					position: fixed;
					inset: 0;
					z-index: 12;
					transform: translateX(min(80vw, 320px));
					cursor: pointer;
				}
				body:has(#mobile-toc-toggle-state:checked) .mobile-page-dismiss {
					display: block;
				}
				.toc-mobile {
					display: block;
					position: fixed;
					top: 0;
					bottom: 0;
					left: 0;
					z-index: 1;
					width: min(80vw, 320px);
					box-sizing: border-box;
					overflow-y: auto;
					padding: 24px 20px;
					background: var(--brand);
					color: var(--bg);
					overscroll-behavior: contain;
				}
				.toc-mobile :global(.toc),
				.toc-mobile :global(.toc summary),
				.toc-mobile :global(.toc a) {
					color: var(--bg);
				}
				.toc-mobile :global(.toc a:hover) {
					background-color: rgba(250, 248, 241, 0.16);
					box-shadow: 0 0 0 4px rgba(250, 248, 241, 0.16);
					color: var(--bg);
				}
				.toc-mobile :global(.toc a.is-active) {
					background-color: var(--bg);
					box-shadow: 0 0 0 4px var(--bg);
					color: var(--brand);
				}
				.mobile-toc-toggle {
					position: fixed;
					right: 16px;
					bottom: calc(24px + env(safe-area-inset-bottom));
					z-index: 13;
					display: grid;
					place-items: center;
					width: 48px;
					height: 48px;
					padding: 0;
					border: 1px solid rgba(var(--gray), 0.24);
					border-radius: 8px;
					background: var(--bg);
					color: rgb(var(--gray-dark));
					box-shadow: 0 4px 12px rgba(var(--gray), 0.12);
					cursor: pointer;
				}
				.mobile-toc-toggle svg {
					width: 22px;
					height: 22px;
					fill: none;
					stroke: currentColor;
					stroke-linecap: round;
					stroke-linejoin: round;
					stroke-width: 2;
				}
				.mobile-toc-close,
				.mobile-toc-toggle-state {
					display: none;
				}
				body:has(#mobile-toc-toggle-state:checked) .mobile-toc-open {
					display: none;
				}
				body:has(#mobile-toc-toggle-state:checked) .mobile-toc-close {
					display: grid;
				}
				.mobile-toc-toggle:focus-visible {
					outline: 3px solid rgba(var(--gray), 0.32);
					outline-offset: 3px;
				}
				.prose {
					font-size: 16px;
				}
				.title h1 {
					font-size: 24px;
				}
				@media (prefers-reduced-motion: reduce) {
					.page-frame,
					.mobile-page-underlay {
						transition: none;
					}
				}
			}
		</style>
	</head>

	<body>
		{
			hasTableOfContents && (
				<>
					<input
						class="mobile-toc-toggle-state"
						id="mobile-toc-toggle-state"
						type="checkbox"
						aria-hidden="true"
						data-mobile-toc-toggle-state
					/>
					<aside class="toc-mobile" id="mobile-toc-panel" aria-label="モバイル目次">
						<TableOfContents headings={headings} />
					</aside>
					<label
						class="mobile-toc-toggle mobile-toc-open"
						for="mobile-toc-toggle-state"
						aria-label="目次を開く"
						aria-controls="mobile-toc-panel"
					>
						<svg viewBox="0 0 24 24" aria-hidden="true">
							<path d="M4 6h16" />
							<path d="M4 12h16" />
							<path d="M4 18h10" />
						</svg>
					</label>
					<label
						class="mobile-toc-toggle mobile-toc-close"
						for="mobile-toc-toggle-state"
						aria-label="目次を閉じる"
						aria-controls="mobile-toc-panel"
					>
						<svg viewBox="0 0 24 24" aria-hidden="true">
							<path d="M18 6 6 18" />
							<path d="m6 6 12 12" />
						</svg>
					</label>
				</>
			)
		}
		{
			hasTableOfContents && (
				<>
					<div class="mobile-page-underlay" aria-hidden="true" />
					<label
						class="mobile-page-dismiss"
						for="mobile-toc-toggle-state"
						aria-label="目次を閉じる"
					/>
				</>
			)
		}
		<div class="page-frame">
			<Header />
			<main>
				<article data-pagefind-body>
					<div class="article-shell">
						<aside class="toc-desktop">
							<TableOfContents headings={headings} />
						</aside>
						<div class="prose">
							<div class="title">
								<h1>{title}</h1>
								<div class="date">
									<FormattedDate date={pubDate} />
									{
										updatedDate && (
											<div class="last-updated-on">
												Last updated on <FormattedDate date={updatedDate} />
											</div>
										)
									}
								</div>
								{
									tags.length > 0 && (
										<div class="tags">
											{tags.map((tag) => (
												<TagLink tag={tag} />
											))}
										</div>
									)
								}
							</div>
							<slot />
							<ShareButton title={title} />
						</div>
					</div>
				</article>
			</main>
			<Footer />
		</div>
		<script>
			const mobileTocToggleState = document.querySelector<HTMLInputElement>(
				'[data-mobile-toc-toggle-state]',
			);
			const mobileToc = document.getElementById('mobile-toc-panel');

			mobileToc?.querySelectorAll('a').forEach((link) => {
				link.addEventListener('click', (event) => {
					const targetId = decodeURIComponent(link.hash.slice(1));
					const target = targetId ? document.getElementById(targetId) : null;

					if (!target) {
						if (mobileTocToggleState) {
							mobileTocToggleState.checked = false;
						}
						return;
					}

					event.preventDefault();
					target.scrollIntoView({ block: 'start' });

					if (mobileTocToggleState) {
						mobileTocToggleState.checked = false;
					}
				});
			});
		</script>
	</body>
</html>
